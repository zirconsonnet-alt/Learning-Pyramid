services:
  postgres:
    image: postgres:16
    ports:
      - "${LEARNINGPYRAMID_TEST_POSTGRES_PORT:-15432}:5432"
    environment:
      POSTGRES_DB: ${LEARNINGPYRAMID_POSTGRES_DB:-learningpyramid_test}
      POSTGRES_USER: ${LEARNINGPYRAMID_POSTGRES_USER:-learningpyramid}
      POSTGRES_PASSWORD: ${LEARNINGPYRAMID_POSTGRES_PASSWORD:-learningpyramid}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${LEARNINGPYRAMID_POSTGRES_USER:-learningpyramid} -d ${LEARNINGPYRAMID_POSTGRES_DB:-learningpyramid_test}"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - ./data/postgres-test:/var/lib/postgresql/data
