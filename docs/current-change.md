# Current Change

更新时间：2026-05-14

## 1. 当前用户要求

- FAQ 在第一条“创建项目前需要干什么？”后面增加一条：
  - 问：绑定的视频目录需要符合什么要求？
  - 答：同构上推模式下，请按照章节结构，分层存放视频。

## 2. 本次实际修改文件

- `docs/guide-faq.md`
- `frontend/src/views/home/HomePage.tsx`
- `docs/current-change.md`

## 3. 每个文件为什么修改

- `docs/guide-faq.md`：指南页 FAQ 的内容源，按要求在第一条后插入新 FAQ。
- `frontend/src/views/home/HomePage.tsx`：首页 FAQ 使用独立 `faqItems`，同步插入同一条，避免两个用户可见 FAQ 分叉。
- `docs/current-change.md`：记录当前任务范围。

## 4. 行为语义是否变化

- 是。用户可见 FAQ 新增一条关于绑定视频目录结构的说明。
- 否。不改变学习材料绑定、同构上推、目录扫描或复习生成逻辑。

## 5. 是否做了重构，以及为什么

- 否。本次只做 FAQ 文案补充。

## 6. 未修改哪些相关内容，以及为什么

- 不修改同构上推实现和导入规则，因为本次只要求增加 FAQ。
- 不新增 FAQ 数据抽象，因为当前改动范围很小，新增抽象会扩大维护面。

## 7. 是否影响 API、架构、部署、数据结构、UI、测试

- API：否。
- 架构：否。
- 部署：否。
- 数据结构：否。
- UI：是，首页和指南页 FAQ 文案增加一条。
- 测试：否，未新增或修改测试。

## 8. 当前风险点和不确定项

- 无。

## 9. 仍需用户确认的问题

- 无。

## 10. 污染风险检查

- 是否新增特殊分支：否
- 是否新增隐式约定：否
- 是否新增重复逻辑：否
- 是否新增 fallback / shim / legacy：否
- 是否修改无关代码：否
- 是否破坏现有抽象边界：否
- 是否可能误导未来维护：否

## 11. 验证状态

- `pnpm exec tsc -b --noEmit`（在 `frontend/` 下运行）：通过。
- `git diff --check`：通过，仅有 LF/CRLF 提示。
