# Current Change

更新时间：2026-05-11

## 1. 当前用户要求

- 会员中心右侧“会员权益”和“我的优惠券”标题设计要一致。
- 优化优惠券卡片下方两行说明文字的样式。

## 2. 本次实际修改文件

- `frontend/src/views/membership/MembershipPage.tsx`
- `docs/current-change.md`

## 3. 每个文件为什么修改

- `frontend/src/views/membership/MembershipPage.tsx`：给“会员权益”标题补同尺寸图标，与“我的优惠券”标题结构对齐；新增局部 `CouponMetaLines` 展示组件，把优惠券卡片下方说明从长句改成更短、更规整的两行信息，并复用到主面板和券包弹窗。
- `docs/current-change.md`：记录本次 UI 变更范围、行为影响和验证结果。

## 4. 行为语义是否变化

否。只改变会员中心右侧面板的展示结构和文案样式，不改变优惠券、会员权益、支付、提现或邀请数据。

## 5. 是否做了重构，以及为什么

做了局部展示层整理。`CouponMetaLines` 只封装同文件内重复的优惠券说明行，避免主面板和券包弹窗出现两套样式。

## 6. 未修改哪些相关内容，以及为什么

- 未修改优惠券状态、来源、过期时间、金额等业务含义。
- 未修改会员权益条目内容，只补标题图标。
- 未调整会员中心左侧佣金、提现区域，避免扩大 UI 范围。
- 未修改购买弹窗里的优惠券选择卡片；本次截图问题在会员中心右侧面板和券包展示。

## 7. 是否影响 API、架构、部署、数据结构、UI、测试

- API：无变化。
- 架构：无变化。
- 部署：需要重新构建并同步前端资源后线上生效。
- 数据结构：无变化。
- UI：标题图标一致；优惠券说明文字样式更新。
- 测试：未新增自动化测试。

## 8. 当前风险点和不确定项

- 图标选择为 `Sparkles`，用于表达会员权益；若需要更偏权益/徽章语义的图标，可再替换。

## 9. 仍需用户确认的问题

无。

## 10. 验证结果

- `pnpm --dir frontend build`：通过；Vite 输出既有 chunk size warning。
- `git diff --check`：通过；仅提示 `docs/current-change.md` 和 `frontend/src/views/membership/MembershipPage.tsx` 下次 Git 触碰时 LF 会替换为 CRLF。

## 11. 污染风险检查

- 是否新增特殊分支：否
- 是否新增隐式约定：否
- 是否新增重复逻辑：否
- 是否新增 fallback / shim / legacy：否
- 是否修改无关代码：否
- 是否破坏现有抽象边界：否
- 是否可能误导未来维护：否
