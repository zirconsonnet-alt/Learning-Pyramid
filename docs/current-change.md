# 当前变更：离线课程包导入实现计划

## 当前用户要求

- 用户确认采用“绑定线上项目的 App 内离线课程包”方案。
- 将已确认设计整理成可执行实现计划，供后续按任务实现。
- 当前阶段不直接实现代码。

## 根因判断

- 手机离线试用需要完整课程素材包，而不是单独字幕投递。
- 离线课程包必须绑定线上 `{subjectId, scopedProjectId}`，避免产生第二套本地课程系统。
- 实现跨桌面字幕工具、临时局域网服务、移动端下载、存储和播放，必须先有清晰任务边界。

## 修改前判断

- 当前只收尾计划文档，不改业务代码。
- 计划应明确不扫描手机系统文件、不写公共目录、不做公网中继、不引入 fallback/shim/legacy。
- 计划中若遇到 Expo SDK 55 大文件、空间检查或本地文件 API 能力不足，应暂停确认，不能偷偷降级实现。

## 本次实际修改文件

- `docs/superpowers/plans/2026-05-20-offline-course-package-import.md`：新增离线课程包导入实现计划，覆盖桌面 manifest、临时服务、字幕工具入口、移动端 schema/storage/downloader/UI/playback 和文档验证任务。
- `docs/current-change.md`：覆盖为当前计划收尾工作单。

## 行为语义是否变化

- 当前未修改运行时代码，行为语义未变化。
- 后续计划中的目标语义是：离线课程包作为线上项目的移动端本地素材源，不作为独立课程系统。

## 重构说明

- 当前未做重构。
- 计划中要求后续实现以小文件和明确边界拆分，避免把下载、存储、播放选择混在页面组件里。

## 未修改内容

- 未修改移动端 UI 当前实现。
- 未修改字幕工具运行逻辑。
- 未修改 API、数据库、部署、构建配置。
- 未修改已有脏工作区文件。

## 影响范围

- 当前仅影响文档和后续实现计划。
- 后续执行计划会影响字幕工具、移动端离线课程库、移动端播放器和移动端项目设置入口。

## 当前风险点和不确定项

- Expo-managed 环境对剩余空间查询、后台下载、大文件断点续传和本地视频 URI 的支持需要在实现时验证。
- 局域网导入受防火墙、路由器隔离和同网段限制影响；第一版不做公网中继。

## 仍需用户确认的问题

- 无。用户已确认采用方案 1。

## 验证记录

- 已运行：计划占位与 Python 禁用 future import 扫描。
  - 结果：未发现禁用写法或占位实现项。
- 已运行：`git diff --check -- docs/superpowers/plans/2026-05-20-offline-course-package-import.md docs/current-change.md`
  - 结果：通过；仅有 Git LF/CRLF 转换提示。

## 污染风险检查

- 是否新增特殊分支：否
- 是否新增隐式约定：否
- 是否新增重复逻辑：否
- 是否新增 fallback / shim / legacy：否
- 是否修改无关代码：否
- 是否破坏现有抽象边界：否
- 是否可能误导未来维护：否
